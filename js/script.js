const observer=new IntersectionObserver(e=>{e.forEach(t=>{if(t.isIntersecting)t.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));document.querySelectorAll('details').forEach(e=>{e.addEventListener('toggle',()=>{if(e.open)document.querySelectorAll('details').forEach(t=>{if(t!==e)t.open=false})})});document.querySelector('.burger').addEventListener('click',()=>document.querySelector('.navlinks').classList.toggle('open'));const audioBtn=document.querySelector('#audioBtn');let ctx,gain,playing=false,nodes=[];function startAmbience(){ctx=new(window.AudioContext||window.webkitAudioContext)();gain=ctx.createGain();gain.gain.value=.035;gain.connect(ctx.destination);[55,82.41,110,146.83].forEach((f,i)=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type=i%2?'triangle':'sine';o.frequency.value=f;g.gain.value=.08/(i+1);o.connect(g);g.connect(gain);o.start();nodes.push(o)});const b=ctx.createBuffer(1,ctx.sampleRate*2,ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=(Math.random()*2-1)*.18;const n=ctx.createBufferSource(),filter=ctx.createBiquadFilter(),ng=ctx.createGain();n.buffer=b;n.loop=true;filter.type='lowpass';filter.frequency.value=420;ng.gain.value=.045;n.connect(filter);filter.connect(ng);ng.connect(gain);n.start();nodes.push(n);playing=true;audioBtn.textContent='Ambiance ON';audioBtn.classList.add('active')}function stopAmbience(){if(!ctx)return;nodes.forEach(n=>{try{n.stop()}catch(e){}});nodes=[];ctx.close();ctx=null;playing=false;audioBtn.textContent='Ambiance OFF';audioBtn.classList.remove('active')}audioBtn.addEventListener('click',()=>playing?stopAmbience():startAmbience());